# =============================================================================
# Environment Variables for GenMedia Frontend
# =============================================================================
#
# SETUP:
#   1. Copy this file to .env.local for development
#   2. Copy this file to .env.production for production deployments
#   3. Fill in your actual values (especially VITE_FIREBASE_API_KEY)
#
# FILES:
#   .env.local       → Used for local development (npm run dev)
#   .env.production  → Used for production deployments (./deploy.sh)
#   .env             → Fallback if neither above exists
#
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Configuration (Frontend)
# -----------------------------------------------------------------------------
# These are baked into the frontend JS at build time.
# Get these from: Firebase Console > Project Settings > General > Your apps
#
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=genmediastudio.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=genmediastudio
VITE_FIREBASE_STORAGE_BUCKET=genmediastudio.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=856765593724
VITE_FIREBASE_APP_ID=1:856765593724:web:2d56922818e4dd876ff1f9
VITE_FIREBASE_MEASUREMENT_ID=G-M4801D5V62

# -----------------------------------------------------------------------------
# Access Control
# -----------------------------------------------------------------------------
# Comma-separated list of allowed email addresses for authentication.
# Users not on this list will be denied access after Google sign-in.
#
VITE_ALLOWED_EMAILS=user1@example.com,user2@example.com

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
# REQUIRE_AUTH: Set to 'true' to require Firebase authentication
#   - Development: 'false' (easier testing, no auth needed)
#   - Production: 'true' (enforces authentication)
#
REQUIRE_AUTH=false

# ALLOWED_ORIGINS: Comma-separated list of allowed CORS origins
#   - Development: localhost ports are allowed by default
#   - Production: Add your Cloud Run URL
#   - Set to '*' to allow all origins (not recommended for production)
#
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://genmedia-frontend-otfo2ctxma-uc.a.run.app

# GCP_PROJECT_ID: Google Cloud project ID (only needed for production)
# If not set, deploy.sh will use gcloud's default project
#
# GCP_PROJECT_ID=genmediastudio
